select
    afs.oec,
    afs.company_id,
    afs.asset_id,
    afs.make,
    afs.model,
    afs.asset_type,
    afs.first_rental,
    afs.year,
    afs.market_id,
    afs.contractor_owned_flag,
    afs.asset_type_adj,
    afs.sage_operating_type,
    afs.financing_facility_type,
    afs.lender,
    afs.financial_schedule_id,
    afs.phoenix_id,
    afs.commencement_date,
    afs.sage_account_number,
    afs.finance_status,
    afs.owned_status,
    afs.purchase_date,
    afs.date_created,
    afs.nbv,
    afs.date::date as date,
    afs.category,
    afs.snapshot_date,
    afs.aphl_status,
    afs."NLV/NBV%",
    afs."NEW/USED",
    afs.new_last_appraisal_date
from {{ source('analytics_public', 'asset_financing_snapshots') }} as afs
