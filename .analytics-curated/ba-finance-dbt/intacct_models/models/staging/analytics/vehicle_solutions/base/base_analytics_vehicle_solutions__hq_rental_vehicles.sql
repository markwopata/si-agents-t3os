select
    hrv.id,
    regexp_substr(
        to_varchar(hrv.current_vehicle_information),
        '[''"]reservation_id[''"]\\s*:\\s*(\\d+)',
        1, 1, 'es', 1
    )::int as reservation_id,
    hrv.label,
    hrv.vehicle_key,
    hrv.vehicle_model_id,
    hrv.vehicle_class_id,
    hrv.vehicle_type_id,
    hrv.year,
    hrv.color,
    hrv.vin,
    hrv.plate,
    hrv.plate_expiration_date,
    hrv.odometer,
    hrv.status,
    hrv.available_date,
    hrv.fuel_level,
    hrv.registration_expiration_date,
    hrv.force_availability_due_to_registration_date,
    hrv.inspection_expiration_date,
    hrv.force_availability_due_to_inspection_date,
    hrv.available,
    hrv.old_current_vehicle_information_id,
    hrv.current_vehicle_information_id,
    hrv.created_by,
    hrv.updated_by,
    hrv.deleted_at,
    hrv.created_at,
    hrv.updated_at,
    hrv.available_until,
    hrv.current_location_id,
    hrv.date_of_last_maintenance,
    hrv.odometer_at_last_maintenance,
    hrv.tour_operator_id,
    hrv.commission_amount,
    hrv.uuid,
    hrv.toll_tag_id,
    hrv.f225,
    hrv.f226,
    hrv.f227,
    hrv.f228,
    hrv.f229,
    hrv.latitude,
    hrv.longitude,
    hrv.hardware_id,
    hrv.reserved_until,
    hrv.reserved_member_id,
    hrv.engine_on,
    hrv.doors_locked,
    hrv.immobilizer,
    hrv.keys_in_holder,
    hrv.speed,
    hrv.rented_days_without_maintenance,
    hrv.vehicle_entry_date,
    hrv.vehicle_exit_date,
    hrv.fuel_type_id,
    hrv.tank_size,
    hrv.sharebox_keys_in_locker,
    hrv.sharebox_cabinet_id,
    hrv.sharebox_reservation_id,
    hrv.sharebox_row,
    hrv.sharebox_column,
    hrv.sharebox_locker_number,
    hrv.status_since,
    hrv.accounting_list_id,
    hrv.accounting_edit_sequence,
    hrv.version,
    hrv.last_location_update_at,
    hrv.last_hardware_update_at,
    hrv.doors_open,
    hrv.accounting_reference_type_id,
    hrv.private_mode_start_date,
    hrv.private_mode_end_date,
    hrv.us_state,
    hrv.outside_geofence,
    hrv.primary_agent_id,
    hrv.vehicle_classes,
    hrv.vehicle_class_label,
    hrv.fuel_level_for_display,
    hrv.status_color,
    hrv.status_label,
    hrv.settings,
    hrv.can_be_relocated,
    hrv.damages_page,
    hrv.vehicle_model,
    hrv.vehicle_class,
    hrv.vehicle_type,
    hrv.current_location,
    hrv.current_vehicle_information,
    hrv.damage_image,
    hrv.fuel_type,
    hrv.tour_operator,
    hrv._es_update_timestamp
from {{ source('analytics_vehicle_solutions', 'hq_rental_vehicles') }} as hrv
