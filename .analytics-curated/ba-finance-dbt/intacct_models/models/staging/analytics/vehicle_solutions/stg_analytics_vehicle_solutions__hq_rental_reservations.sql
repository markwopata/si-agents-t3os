select
    hrr.id,
    hrr.prefixed_id,
    hrr.reservation_id,
    hrr.pick_up_date,
    hrr.initial_pick_up_date,
    hrr.return_date,
    hrr.initial_return_date,
    hrr.rental_duration,
    hrr.flight_arrival_date,
    hrr.cancellation_date,
    hrr.cancellation_comments,
    hrr.cancellation_reason_id,
    hrr.cancelled_by,
    hrr.cancelled_at,
    hrr.brand_id,
    hrr.initial_brand_id,
    hrr.vehicle_class_id,
    hrr.free_vehicle_class_upgrade_id,
    hrr.pick_up_location_id,
    hrr.return_location_id,
    hrr.flight_number,
    hrr.flight_airline,
    hrr.pick_up_location_custom,
    hrr.return_location_custom,
    hrr.local_address,
    hrr.customer_id,
    hrr.currency,
    hrr.rack_price,
    hrr.miscellaneous_price,
    hrr.external_charges_price,
    hrr.discounts_amount,
    hrr.taxes_amount,
    hrr.security_deposit,
    hrr.security_deposit_excess,
    hrr.manual_discount,
    hrr.manual_discount_percentage,
    hrr.manual_discount_is_percentage,
    hrr.government_tax,
    hrr.abb_tax,
    hrr.location_fee,
    hrr.outstanding_balance,
    hrr.status,
    hrr.completed_at,
    hrr.completed_by,
    hrr.reopened_by,
    hrr.reopened_at,
    hrr.after_sale_email_sent,
    hrr.forced_security_deposit_id,
    hrr.reservation_from_website,
    hrr.created_by,
    hrr.updated_by,
    hrr.deleted_at,
    hrr.created_at,
    hrr.updated_at,
    hrr.uuid,
    hrr.uuid_short,
    hrr.no_show_fee,
    hrr.cancellation_fee,
    hrr.direct_booker_ip,
    hrr.country_code,
    hrr.region_code,
    hrr.zip_code,
    hrr.booker_information,
    hrr.percentage_discount_from_to_commission,
    hrr.was_reopened,
    hrr.has_package_reservation,
    hrr.primary_agent_id,
    hrr.secondary_agents,
    hrr.primary_return_agent_id,
    hrr.secondary_return_agents,
    hrr.walk_in_customer,
    hrr.security_deposit_automatically_charged,
    hrr.security_deposit_paid,
    hrr.exchange_rate_from_currency_to_base,
    hrr.digital_signature,
    hrr.reservation_commission,
    hrr.loan_reservation,
    hrr.distance_limit_per_day,
    hrr.distance_limit_per_week,
    hrr.distance_limit_per_month,
    hrr.return_reminder_email_sent,
    hrr.last_day_counts_for_calculations,
    hrr.reservation_interval,
    hrr.digital_signature_for_initials,
    hrr.signed_at,
    hrr.initials_signed_at,
    hrr.base_reservation_commission,
    hrr.commission_type,
    hrr.disable_renewal,
    hrr.renew_contract_x_days_in_advance,
    hrr.security_deposit_refunded,
    hrr.security_deposit_release_attempts,
    hrr.referral,
    hrr.security_deposit_charged_amount,
    hrr.security_deposit_refunded_amount,
    hrr.distance_manually_updated,
    hrr.transaction_fees_price,
    hrr.distance_limit,
    hrr.force_renewal_to_x_days,
    hrr.reserved_from,
    hrr.percentage_charges_over_discounted_rate,
    hrr.damages_price,
    hrr.charge_distance_when_limit_is_zero,
    hrr.dont_include_location_fee_in_tax_calculation,
    hrr.reservation_type,
    hrr.ota_reservation_type,
    hrr.ota_reservation_id,
    hrr.user_digital_signature_id,
    hrr.user_digital_signature_for_initials_id,
    hrr.car_sharing_vehicle_returned,
    hrr.total_price,
    hrr.authorization_transaction_id,
    hrr.reservation_started_at,
    hrr.reservation_ended_at,
    hrr.flight_departure_date,
    hrr.flight_departure_number,
    hrr.flight_departure_airline,
    hrr.total_rack_rate,
    hrr.total_insurances,
    hrr.total_extras,
    hrr.total_taxes,
    hrr.total_revenue,
    hrr.total_paid,
    hrr.total_refunded,
    hrr.total_days,
    hrr.rental_user_id,
    hrr.dont_include_vehicle_damages_in_location_fee,
    hrr.dont_include_vehicle_damages_in_taxes,
    hrr.internal_reservation,
    hrr.total_location_fee,
    hrr.total_abb_tax,
    hrr.total_government_tax,
    hrr.include_external_charges_in_location_fee,
    hrr.include_external_charges_in_taxes,
    hrr.last_payment_request_sent_at,
    hrr.last_payment_at,
    hrr.user_agent,
    hrr.security_deposit_was_manually_changed,
    hrr.security_deposit_excess_was_manually_changed,
    hrr.language,
    hrr.preferred_credit_card_id,
    hrr.external_charge_account_id,
    hrr.multiple_seasons_for_weekdays,
    hrr.mandatory_charges_based_on_combinations_enabled,
    hrr.website_internal_confirmation_email_sent,
    hrr.confirmation_email_sent,
    hrr.round_amounts_to_two_decimals_on_calculations,
    hrr.max_hours_before_daily_price,
    hrr.max_hours_before_daily_price_additional_charges,
    hrr.rate_type_id,
    hrr.without_mandatory_charges,
    hrr.renew_contract_x_hours_after_planned_return_date,
    hrr.extension_paid_until,
    hrr.has_pending_extensions,
    hrr.grace_period_hours_for_picking_up_cars,
    hrr.grace_period_hours_for_returning_cars,
    hrr.no_show_by,
    hrr.no_show_at,
    hrr.check_value_pricing_on_daily_vs_weekly_rates,
    hrr.availability_percentage_at_creation,
    hrr.enable_multiple_seasons_bookings,
    hrr.enable_season_rates_based_on_multipliers,
    hrr.forced_close_reason,
    hrr.driver_validated,
    hrr.ready_for_pick_up,
    hrr.ready_for_pick_up_approval_user_id,
    hrr.ready_for_pick_up_at,
    hrr.enable_self_service_test_mode,
    hrr.f365,
    hrr.f367,
    hrr.f368,
    hrr.signature_metadata,
    hrr.initials_signature_metadata,
    hrr.f447,
    hrr.f448,
    hrr.enable_self_service,
    hrr.enable_auto_pick_up,
    hrr.enable_auto_return,
    hrr.reservation_email_template_name,
    hrr.disable_all_emails,
    hrr.pick_up_location_custom_latitude,
    hrr.pick_up_location_custom_longitude,
    hrr.return_location_custom_latitude,
    hrr.return_location_custom_longitude,
    hrr.include_vehicle_damages_in_revenue,
    hrr.reserved_until,
    hrr.self_service_rental_picked_up,
    hrr.self_service_rental_returned,
    hrr.self_service_billed,
    hrr.insurance_verified,
    hrr.ota_billed,
    hrr.ota_price,
    hrr.last_auto_charge_at,
    hrr.last_auto_payment_request_at,
    hrr.tour_operator,
    hrr.pick_up_location_label,
    hrr.return_location_label,
    hrr.reservation_type_label,
    hrr.brand_label,
    hrr.total_price_without_taxes,
    hrr.pick_up_time,
    hrr.return_time,
    hrr.commission,
    hrr.pick_up_location,
    hrr.pick_up_location_id,
    hrr.return_location,
    hrr.brand,
    hrr.customer,
    hrr.additional_drivers,
    hrr.vehicle_class,
    hrr.free_vehicle_class_upgrade,
    hrr.rack_rate_details,
    hrr.all_additional_charges,
    hrr.reservation_vehicle_information,
    hrr.active_vehicle_information,
    hrr.package_reservation,
    hrr._es_update_timestamp
from {{ ref('base_analytics_vehicle_solutions__hq_rental_reservations') }} as hrr
qualify row_number() over (partition by hrr.id order by hrr._es_update_timestamp desc) = 1
