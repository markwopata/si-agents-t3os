select
    creditnotelineid as credit_note_line_id,
    vatamount as vat_amount,
    additionalcharge as additional_charge,
    settlementamountincurrency as settlement_amount_in_currency,
    settlementdiscount as settlement_discount,
    manualdiscount1amount as manual_discount_1_amount,
    vatamountincurrency as vat_amount_in_currency,
    additionalcost as additional_cost,
    taxgroupid as tax_group_id,
    unitamountdisplay as unit_amount_display,
    manualdiscount1amountincurrency as manual_discount_1_amount_in_currency,
    branchid as bt_branch_id,
    unitamount as unit_amount,
    restockchargeincurrency as restock_charge_in_currency,
    disablerebateaccruals as disable_rebate_accruals,
    manualdescription as manual_description,
    manuallinepricetocalculate as manual_line_price_to_calculate,
    totalcost as total_cost,
    sellingpriceruleid as selling_price_rule_id,
    taxcodes as tax_codes,
    includeavocost as include_avo_cost,
    wmscreatedfromrn as wms_created_from_rn,
    adjustedforpricecorrectionsamount as adjusted_for_price_corrections_amount,
    vmireconciled as vmi_reconciled,
    returnquantitydisplay as return_quantity_display,
    internalcosttotalmargin as internal_cost_total_margin,
    totalvmicostincurrency as total_vmi_cost_in_currency,
    creditnoteid as credit_note_id,
    promotionid as promotion_id,
    unitamountincurrency as unit_amount_in_currency,
    couponid as coupon_id,
    internaltotalcostprice as internal_total_cost_price,
    quantityfromscale as quantity_from_scale,
    manualdiscount2amount as manual_discount_2_amount,
    exportedtodatawarehouse as exported_to_data_warehouse,
    originallineid as original_line_id,
    generatedfromcontractinvoicing as generated_from_contract_invoicing,
    totalweight as total_weight,
    totalvmicost as total_vmi_cost,
    invoicelinetaxincluded as invoice_line_tax_included,
    customerreturnlineid as customer_return_line_id,
    promotionpointsadjustment as promotion_points_adjustment,
    totalvolume as total_volume,
    wmsprocessed as wms_processed,
    totalmargin as total_margin,
    noofpieces as no_of_pieces,
    description as description,
    quantitydisplay as quantity_display,
    vmiruleid as vmi_rule_id,
    displayinputperid as display_input_per_id,
    manualorderlinetypeid as manual_order_line_type_id,
    totaltaxincurrency as total_tax_in_currency,
    linenumber as line_number,
    perid as per_id,
    productbatchoption as product_batch_option,
    returntosupplier as return_to_supplier,
    pointsearnedadjustment as points_earned_adjustment,
    manualdiscount2amountincurrency as manual_discount_2_amount_in_currency,
    itemnumber as item_number,
    quantitytally as quantity_tally,
    vmisupplierinvoicedocumentid as vmi_supplier_invoice_document_id,
    orderlineid as order_line_id,
    totaltax as total_tax,
    wmsunitnumber as wms_unit_number,
    nonsalesamountincurrency as non_sales_amount_in_currency,
    createdfromcontractinvoicinglineid as created_from_contract_invoicing_line_id,
    linetype as line_type,
    notes as notes,
    settlementamount as settlement_amount,
    salesanalysiscodeid as sales_analysis_code_id,
    totalreportingcost as total_reporting_cost,
    totallength as total_length,
    nonsalesamount as non_sales_amount,
    returnquantity as return_quantity,
    manualcode as manual_code,
    invoicelineid as invoice_line_id,
    quantity as quantity,
    totalamountincurrency as total_amount_in_currency,
    productid::integer as product_id,
    totalamount as total_amount,
    correctedunitamount as corrected_unit_amount,
    inputperid as input_per_id,
    scalegrossweight as scale_gross_weight,
    pointsredeemedadjustment as points_redeemed_adjustment,
    vatrateid as vat_rate_id,
    restockcharge as restock_charge,
    vmiexchangerate as vmi_exchange_rate,
    _fivetran_deleted as _fivetran_deleted,
    _fivetran_synced as _fivetran_synced

from {{ source('analytics_bt_dbo', 'creditnoteline') }}
