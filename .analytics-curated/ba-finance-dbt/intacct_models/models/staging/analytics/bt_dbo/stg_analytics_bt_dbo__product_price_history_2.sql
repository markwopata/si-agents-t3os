select
    p.productpricehistory2id as product_price_history_2_id,
    p.productbatchid as product_batch_id,
    p.stockactualupdate as stock_actual_update,
    p.transactiontotalpricewithadd as transaction_total_price_with_add,
    p.sourcetype as source_type,
    p.transactionunitpricewithadd as transaction_unit_price_with_add,
    p.branchid as bt_branch_id,
    p.perid as per_id,
    p.newtotalstockvaluation as new_total_stock_valuation,
    p.transactionquantity as transaction_quantity,
    p.userid as user_id,
    p.sourcenumber as source_number,
    p.changedatetime as change_datetime,
    p.oldprice as old_price,
    p.transactionunitprice as transaction_unit_price,
    p.negativeinventoryadjustment as negative_inventory_adjustment,
    p.oldstockactual as old_stock_actual,
    p.sourceid as source_id,
    p.oldperid as old_per_id,
    p.productid as product_id,
    p.transactiontotalprice as transaction_total_price,
    p.newstockactual as new_stock_actual,
    p.length as length,
    p.price as price,
    p.postingdate as posting_date,
    p.pricetype as price_type,
    p.purchasepricevariance as purchase_price_variance,
    p._fivetran_deleted as _fivetran_deleted,
    p._fivetran_synced as _fivetran_synced
from {{ source('analytics_bt_dbo', 'productpricehistory2') }} as p
