SELECT
    si.record_effective_date,
    si.record_ineffective_date,
    si.user_id,
    si.employee_id,
    si.name,
    si.email_address,
    si.region_dated,
    si.region_name_dated,
    si.district_dated, 
    si.home_market_id_dated,
    si.home_market_dated,
    si.date_hired_initial, 
    si.start_date_as_salesperson,
    si.date_rehired_present,
    si.date_terminated_present, 
    si.employee_status_present,
    si.employee_title_dated,
    si.employee_title_dated_is_salesperson, 
    si.salesperson_jurisdiction_dated,
    si.direct_manager_user_id_present,
    si.direct_manager_employee_id_present,
    si.direct_manager_name_present,
    si.direct_manager_email_present,
    si.first_date_as_tam         
FROM {{ source('analytics_bi_ops', 'salesperson_info') }} as si
