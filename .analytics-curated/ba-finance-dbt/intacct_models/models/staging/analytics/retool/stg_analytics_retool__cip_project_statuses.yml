---
version: 2
models:
  - name: stg_analytics_retool__cip_project_statuses
    description: Model for analytics.retool.cip_project_statuses
    config:
      materialized: view
    columns:
      - name: project_status_id
        description: 'Unique identifier for each CIP project status'
        data_tests:
          - not_null
          - unique

      - name: project_status_name
        description: 'Name of the project status'

      - name: description
        description: 'Detailed description of the project status'

      - name: date_created
        description: 'Timestamp when the record was created'

      - name: created_by
        description: 'User who created the record'

      - name: date_updated
        description: 'Timestamp of last update'

      - name: updated_by
        description: 'User who last updated the record'
