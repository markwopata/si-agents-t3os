SELECT
    dpo.purchase_order_key,
    dpo.purchase_order_source,
    dpo.purchase_order_id,
    dpo.purchase_order_market_key,
    dpo.purchase_order_vendor_key,
    dpo.purchase_order_approved_at_date_key,
    dpo.purchase_order_created_at_date,
    dpo.purchase_order_modified_date,
    dpo.purchase_order_submitted_date_key,
    dpo.purchase_order_approved_by_user_key,
    dpo.purchase_order_created_by_user_key,
    dpo.purchase_order_submitted_by_user_key,
    dpo.purchase_order_type_id,
    dpo.purchase_order_type,
    dpo.purchase_order_net_term_id,
    dpo.purchase_order_net_term_description,
    dpo.purchase_order_days_in_net_term,
    dpo.purchase_order_note,
    dpo.purchase_order_has_net_terms_flag,
    dpo.purchase_order_is_approved_flag,
    dpo.purchase_order_note_indicates_void_or_delete,
    dpo.purchase_order_recordtimestamp
FROM {{ source('fleet_optimization_gold', 'dim_purchase_orders') }} as dpo
