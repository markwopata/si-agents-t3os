{{ 
    config(
    materialized='table'
    ) 
}}

SELECT
    ap._assets_last_instance_indicator,
    ap._assets_effective_start_utc_datetime,
    ap._assets_effective_end_utc_datetime,
    ap._assets_effective_delete_utc_datetime,
    ap.asset_id,
    ap.analog_to_digital_fuel_level_curve_id,
    ap.asset_settings_id,
    ap.asset_type_id,
    ap.available_for_rent,
    ap.available_to_rapid_rent,
    ap.battery_voltage_type_id,
    ap.camera_id,
    ap.category_id,
    ap.company_id,
    ap.custom_name,
    ap.deleted,
    ap.description,
    ap.dot_number_id,
    ap.driver_name,
    ap.elogs_certified,
    ap.elog_device_id,
    ap.equipment_class_id,
    ap.equipment_condition_id,
    ap.equipment_make_id,
    ap.equipment_model_id,
    ap.hours,
    ap.inventory_branch_id,
    ap.location_id,
    ap.maintenance_group_id,
    ap.market_id,
    ap.model,
    ap.name,
    ap.odometer,
    ap.payout_percentage,
    ap.photo_id,
    ap.placed_in_service,
    ap.price_per_day,
    ap.price_per_hour,
    ap.price_per_month,
    ap.price_per_week,
    ap.purchase_price,
    ap.rental_branch_id,
    ap.serial_number,
    ap.service_branch_id,
    ap.service_provider_company_id,
    ap.time_fence_id,
    ap.total_fuel_used_liters,
    ap.total_idle_fuel_used_liters,
    ap.total_idle_seconds,
    ap.tracker_id,
    ap.vin,
    ap.weekly_minimum,
    ap.weight_lbs,
    ap.year,
    ap.date_created,
    ap.date_updated
FROM {{ source('equipmentshare_public__silver', 'assets_pit') }} as ap
