SELECT
    gi.id,
    gi.domain_id,
    gi.invoice_id,
    gi.order_id,
    gi.invoice_no,
    gi.due_date,
    gi.created_date,
    gi.invoice_date,
    gi.billing_approved_date,
    gi.billing_approved,
    gi.start_date,
    gi.end_date,
    gi.rental_amount,
    gi.owed_amount,
    gi.line_item_amount,
    gi.billed_amount,
    gi.rpp_amount,
    gi.tax_amount,
    gi.customer_tax_exempt_status,
    gi.sent,
    gi.sent_date,
    gi.reference,
    gi.paid,
    gi.paid_date,
    gi.salesperson_user_id,
    gi.purchase_order_id,
    gi.ordered_by_user_id,
    gi.company_id,
    gi.credit_note_parent_invoice_id,
    gi.rental_id,
    gi.status,
    gi.type,
    gi.currency_code,
    gi.billing_address,
    gi.deleted_date,
    gi.outstanding,
    gi.due_date_outstanding,
    gi.amount_credited,
    gi._es_update_timestamp
FROM {{ source('es_warehouse_public', 'global_invoices') }} as gi
