SELECT
    asi.asset_id,
    asi.service_interval_id,
    asi.maintenance_group_interval_id,
    asi.maintenance_group_interval_name,
    asi.maintenance_group_id,
    asi.usage_interval_id,
    asi.time_interval_id,
    asi.hours,
    asi.odometer,
    asi.service_interval_name,
    asi.time_unit_id,
    asi.time_value,
    asi.usage_unit_id,
    asi.usage_value,
    asi.service_record_id,
    asi.last_service_usage_value,
    asi.next_service_usage_value,
    asi.current_usage_value,
    asi.until_next_service_usage,
    asi.usage_percentage_remaining,
    asi.usage_percentage,
    asi.last_service_time_value,
    asi.next_service_time_value,
    asi.next_service_time_value_corrected,
    asi.current_time_value,
    asi.time_value_corrected,
    asi.service_time_remaining_in_weeks,
    asi.date_completed,
    asi._next_service_time_value,
    asi._last_service_time_value,
    asi._current_time_value,
    asi.until_next_service_time,
    asi.time_percentage_remaining,
    asi.time_percentage,
    asi.work_order_originator_id,
    asi.work_order_id,
    asi.warn_exceeded,
    asi.trigger_exceeded,
    asi.date_created
FROM {{ source('es_warehouse_public', 'asset_service_intervals') }} as asi
