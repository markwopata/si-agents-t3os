SELECT
    vasi.asset_id,
    vasi.service_interval_id,
    vasi.maintenance_group_interval_id,
    vasi.maintenance_group_interval_name,
    vasi.maintenance_group_id,
    vasi.usage_interval_id,
    vasi.time_interval_id,
    vasi.hours,
    vasi.odometer,
    vasi.service_interval_name,
    vasi.time_unit_id,
    vasi.time_value,
    vasi.usage_unit_id,
    vasi.usage_value,
    vasi.service_record_id,
    vasi.last_service_usage_value,
    vasi.next_service_usage_value,
    vasi.current_usage_value,
    vasi.until_next_service_usage,
    vasi.usage_percentage_remaining,
    vasi.usage_percentage,
    vasi.last_service_time_value,
    vasi.next_service_time_value,
    vasi.next_service_time_value_corrected,
    vasi.current_time_value,
    vasi.time_value_corrected,
    vasi.service_time_remaining_in_weeks,
    vasi.date_completed,
    vasi._next_service_time_value,
    vasi._last_service_time_value,
    vasi._current_time_value,
    vasi.until_next_service_time,
    vasi.time_percentage_remaining,
    vasi.time_percentage,
    vasi.work_order_originator_id,
    vasi.work_order_id,
    vasi.warn_exceeded,
    vasi.trigger_exceeded,
    vasi.date_created
FROM {{ source('es_warehouse_public', 'v_asset_service_intervals') }} as vasi
