SELECT
    giv.id,
    giv.domain_id,
    giv.invoice_id,
    giv.order_id,
    giv.invoice_no,
    giv.due_date,
    giv.created_date,
    giv.invoice_date,
    giv.billing_approved_date,
    giv.billing_approved,
    giv.start_date,
    giv.end_date,
    giv.rental_amount,
    giv.owed_amount,
    giv.line_item_amount,
    giv.billed_amount,
    giv.rpp_amount,
    giv.tax_amount,
    giv.customer_tax_exempt_status,
    giv.sent,
    giv.sent_date,
    giv.reference,
    giv.paid,
    giv.paid_date,
    giv.salesperson_user_id,
    giv.purchase_order_id,
    giv.ordered_by_user_id,
    giv.company_id,
    giv.credit_note_parent_invoice_id,
    giv.rental_id,
    giv.status,
    giv.type,
    giv.currency_code,
    giv.billing_address,
    giv.deleted_date,
    giv.outstanding,
    giv.due_date_outstanding,
    giv._es_update_timestamp
FROM {{ source('es_warehouse_public', 'global_invoices_v1') }} as giv
