SELECT
    vak.asset_id,
    vak.asset_type_id,
    vak.custom_name,
    vak.description,
    vak.year,
    vak.model,
    vak.company_id,
    vak.tracker_id,
    vak.asset_settings_id,
    vak.photo_id,
    vak.time_fence_id,
    vak.equipment_make_id,
    vak.deleted,
    vak.market_id,
    vak.maintenance_group_id,
    vak.camera_id,
    vak.elog_device_id,
    vak.analog_to_digital_fuel_level_curve_id,
    vak.payout_percentage,
    vak.vin,
    vak.driver_name,
    vak.name,
    vak.serial_number,
    vak.price_per_hour,
    vak.price_per_day,
    vak.price_per_week,
    vak.price_per_month,
    vak.available_for_rent,
    vak.purchase_price,
    vak.weekly_minimum,
    vak.weight_lbs,
    vak.category_id,
    vak.location_id,
    vak.equipment_condition_id,
    vak.equipment_model_id,
    vak.elogs_certified,
    vak.service_branch_id,
    vak.inventory_branch_id,
    vak.rental_branch_id,
    vak.placed_in_service,
    vak.available_to_rapid_rent,
    vak.dot_number_id,
    vak.battery_voltage_type_id,
    vak.make,
    vak.asset_class,
    vak.equipment_class_id,
    vak.keypad_id,
    vak.keypad_firmware_id,
    vak.date_created,
    vak.date_updated,
    vak._es_update_timestamp
FROM {{ source('es_warehouse_public', 'v_assets_keypads') }} as vak
