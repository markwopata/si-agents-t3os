with 
    details_final as (
        select * from {{ ref("stg_analytics_commission__retail_commission_details_final") }}
    )
    -- no manual adjustment just yet
    {# ,
    manual_adjustments as (
        select * from {{ ref("stg_analytics_commission__commission_manual_adjustments") }}
    )
select
    commission_id,
    line_item_id,
    salesperson_user_id,
    invoice_id,
    invoice_no,
    order_id,
    order_date,
    market_id,
    market_name,
    parent_market_id,
    parent_market_name,
    region,
    region_name,
    district,
    ship_to_state,
    company_id,
    company_name,
    line_item_type_id,
    line_item_type,
    asset_id,
    invoice_asset_make,
    invoice_class_id,
    invoice_class,
    rental_class_id_from_rental,
    rental_class_from_line_item,
    line_item_amount,
    amount,
    email_address,
    employee_id,
    full_name,
    employee_title,
    transaction_type,
    employee_manager_id,
    employee_manager,
    salesperson_type_id,
    salesperson_type,
    employee_type,
    secondary_rep_count,
    rate_tier_id,
    rate_tier_name,
    book_rate,
    benchmark_rate,
    floor_rate,
    business_segment_id,
    rental_id,
    rental_date_created,
    rental_start_date,
    rental_billed_days,
    cheapest_period,
    quoted_rates,
    override_rate,
    is_exception,
    is_override,
    allocation_cost_center,
    commission_rate,
    transaction_date,
    billing_approved_date,
    split,
    transaction_type_id,
    is_payable,
    reimbursement_factor,
    credit_note_line_item_id::int as credit_note_line_item_id,
    transaction_description,
    commission_month,
    commission_amount,
    paycheck_date,
    hidden,
    manual_adjustment_id,
    commission_type_id
from manual_adjustments

union all #}

select
    COMMISSION_ID,
        LINE_ITEM_ID,
        SALESPERSON_USER_ID,
        INVOICE_ID,
        INVOICE_NO,
        ORDER_ID,
        INVOICE_CREATED_DATE,
        ORDER_DATE,
        MARKET_ID,
        MARKET_NAME,
        PARENT_MARKET_ID,
        PARENT_MARKET_NAME,
        REGION,
        REGION_NAME,
        DISTRICT,
        SHIP_TO_STATE,
        COMPANY_ID,
        COMPANY_NAME,
        LINE_ITEM_TYPE_ID,
        LINE_ITEM_TYPE,
        ASSET_ID,
        INVOICE_ASSET_MAKE,
        INVOICE_CLASS_ID,
        INVOICE_CLASS,
        LINE_ITEM_AMOUNT,
        AMOUNT,
        EMAIL_ADDRESS,
        EMPLOYEE_ID,
        FULL_NAME,
        EMPLOYEE_TITLE,
        TRANSACTION_TYPE,
        EMPLOYEE_MANAGER_ID,
        EMPLOYEE_MANAGER,
        SALESPERSON_TYPE_ID,
        SALESPERSON_TYPE,
        EMPLOYEE_TYPE,
        SECONDARY_REP_COUNT,
        PROFIT_MARGIN,
        NBV,
        FLOOR_RATE,
        BENCHMARK_RATE,
        ONLINE_RATE,
        RATE_TIER_ID,
        RATE_ACHIEVEMENT,
        IS_EXCEPTION,
        ALLOCATION_COST_CENTER,
        COMMISSION_RATE,
        TRANSACTION_DATE,
        BILLING_APPROVED_DATE,
        SPLIT,
        TRANSACTION_TYPE_ID,
        IS_PAYABLE,
        IS_FINALIZED,
        REIMBURSEMENT_FACTOR,
        CREDIT_NOTE_LINE_ITEM_ID,
        TRANSACTION_DESCRIPTION,
        COMMISSION_MONTH,
        COMMISSION_AMOUNT,
        PAYCHECK_DATE,
        HIDDEN,
        MANUAL_ADJUSTMENT_ID,
        commission_type_id
from details_final
