---
version: 2

snapshots:
  - name: scd2_es_warehouse_public__order_salespersons
    description: >
      SCD2 (Type 2) snapshot of the staged es_warehouse.public.order_salespersons table, maintaining point-in-time history of
      order salespersons.
    config:
      enabled: true
    columns:
      - name: dbt_scd_id
        data_tests:
          - unique
      - name: order_salesperson_id
        description: Primary key of the underlying order salesperson record.
        data_tests:
          - not_null
