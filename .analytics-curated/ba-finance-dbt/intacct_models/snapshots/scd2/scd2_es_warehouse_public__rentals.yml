---
version: 2

snapshots:
  - name: scd2_es_warehouse_public__rentals
    description: >
      SCD2 (Type 2) snapshot of the staged es_warehouse.public.rentals table, maintaining point-in-time history of rentals.
    config:
      enabled: true
    columns:
      - name: dbt_scd_id
        data_tests:
          - unique
      - name: rental_id
        description: Primary key of the underlying rental record.
        data_tests:
          - not_null
